services:
  app:
    build: .
    ports:
      - "8080:8000"
    environment:
      SITZUNG_TAGE: ${SITZUNG_TAGE:-30}
      ADRESSEN_TAGE: ${ADRESSEN_TAGE:-30}
      CORS_ORIGINS: ${CORS_ORIGINS:-*}
    volumes:
      - freigaben:/data
    restart: unless-stopped

volumes:
  freigaben:
